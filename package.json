{
  "name": "nextory-homepage",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run prebuild && vite --host 127.0.0.1",
    "prebuild": "node scripts/build-pages.js",
    "build": "npm run prebuild && vite build",
    "preview": "vite preview --host 127.0.0.1"
  },
  "devDependencies": {
    "vite": "^8.1.0"
  }
}
